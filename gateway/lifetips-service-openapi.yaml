swagger: '2.0'
info:
  title: Keugeugeuk User API
  version: 1.0.0
schemes:
  - https
produces:
  - application/json
paths:
  /lifetips:
    get:
      summary: "Get YouTube life tips"
      operationId: "getLifeTips"
      parameters:
        - name: q
          in: query
          required: true
          type: string
      responses:
        200:
          description: "OK"
x-google-backend:
  address: https://lifetips-service-994769112985.asia-northeast1.run.app
  protocol: h2